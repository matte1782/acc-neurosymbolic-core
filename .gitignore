# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# Tooling / caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*
*.local

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Project data (large binaries kept out of git)
*.ifc
*.ifczip
models/
data/raw/

# Diagnostic artifacts (Stage 3 probes — regenerated from the .ifc fixtures, not source)
sandbox/data/*_report*.json
sandbox/probe_*.py

# Track B gate-spike run reports (regenerable: python sandbox/gate_spike.py --live --report ...)
sandbox/spike_report_*.json

# Audit research: the adversarial corpus .ifc are covered by *.ifc above (regenerable via
# research/corpus/gen_adversarial.py); the run output is regenerable too. The pinned
# expected_verdicts.json (the external oracle) IS tracked.
research/corpus/corpus_results.json

# Local LLM weights
*.gguf
*.safetensors
