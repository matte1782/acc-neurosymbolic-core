# Stage 4b — Graph anchoring: the room→occupancy decision flows from a SPARQL query over an rdflib store, the IfcSpace enters the store, controls byte-frozen THROUGH the graph path, with an INDEPENDENT (non-`classify()`-derived) oracle and an HONEST, BOUNDED non-circularity claim

Repo: `acc-neurosymbolic-core` (Slice A). Stage 4 (Parts 1–4) is **done, green, and adversarially verified complete** (3-lens audit, 2026-06-20): record-backed requirement model, externalized + gate-verified applicability/selection table, and a gate-verified 2nd rule (`alloggio monostanza`, honest-undetermined). ADR-005 deferred the graph to **this stage**. The user chose to **build the minimal REAL graph now** — establishing the architecture seam, not because the ~150-rule scale trigger has fired (it has not: 2 rules / 3 fixtures). **This command was itself adversarially pre-reviewed** (3 reviewers, 2026-06-20) and the defects they found are pre-fixed below; read the HONESTY CONTRACT — over-claiming non-circularity is the failure mode this stage must avoid.

> **HONESTY CONTRACT (read first — the pre-review's central correction).** On the **3 current fixtures every room is flat-substring-decidable** (baseline §6), so a graph seeded from the same hints is **verdict-equivalent to the flat table BY CONSTRUCTION** — reproducing the controls proves a *faithful copy of `classify()`*, **not** correctness. Do **NOT** claim "the graph is non-cosmetic because it reproduces the controls" or "non-circular because accessory is statute-anchored." The defensible, *quantified* claim this stage delivers is exactly three things, no more:
> 1. **Seam established:** the room→occupancy decision is now a **SPARQL 1.1 query over an rdflib store into which each IfcSpace is materialized at runtime**, replacing the Python substring branch — an architectural move, demonstrated verdict-neutral.
> 2. **Bounded statute-anchoring:** **4 of 51** occupancy tokens (`corrid/disimpegno/bagno/ripostiglio`, the Art.1 enumeration) are checked against the **statute prose** via `verify_accessory_selection_against_text`; the **other 47 remain `classify()`-derived, reproduced-not-independently-verified**, and the cross-lingual hints stay **declared-debt edges** (baseline §7). Circularity is **bounded, NOT eliminated** — state the 4/51 number verbatim in ADR-006 and the tests.
> 3. **Inference capability demonstrated (synthetically):** a `rdfs:subClassOf+` **divergence room** proves the graph *can* classify a room-type the flat list cannot — demonstrated on a **synthetic** test node, because **no real fixture needs transitive inference** (baseline §6). This is capability-establishment, not production inference. Say so.

> **The two traps the pre-review proved live, and the exact guards (do not weaken).**
> - **Cosmetic / done-when-in-letter.** A fake passes by turning hints into triples but leaving `classify()` Python-backed and never inserting the IfcSpace (the dead-`selection`-block pattern — the compiled rule's `selection` is already unread at runtime, `checker.py:205-223`). GUARD: `classify()`'s substring branch is **deleted**; the verdict **provably flows through the store** (TASK 2 adds a *raise-on-empty-ontology* + *substring-branch-absent* assertion, not just output-equivalence); the IfcSpace nodes are **count-exact + GlobalId-set-equal** in the store, not merely "non-empty".
> - **Circularity ("echo the decomposition", baseline §1 / `parser.py:262-270`).** The 220-row oracle and the controls are **outputs of `classify()`**; reproducing them through SPARQL is **necessary but INSUFFICIENT**. The two *sufficient* legs must be real, not tautological: (a) the reproduction battery is pinned to the **frozen `equiv_oracle.json` occupancy column** and the **statute gate**, NEVER to a hand-authored map of the graph's own output; (b) the divergence room uses a token **machine-proven to be a non-substring of all 51 hints**, compared against the **real `classify()`** (not a strawman), with the `subClassOf` edge proven **load-bearing** (remove it → answer flips to `unknown`).

> **Design choice (minimal, recorded).** The graph carries **occupancy / applicability discovery only** ("which class is this room, so which requirements apply"). Requirement **VALUES stay in `Thresholds.resolve`**, and **monostanza stays OUT of the graph** — the checker-side monostanza constants are **not gate-checked** (ADR-005 (ii)), so promoting them to graph "facts" would launder an unverified transcription; that wiring is a later stage.

> **Working directory.** Repo root `C:/Users/matte/Desktop/Desktop OLD/AI/Università AI/courses/personal_project/industrial.review/acc-neurosymbolic-core`. Paths below are relative; checker/test commands run from `sandbox/`.

## BOOT (read first)
`sandbox/STAGE4_BASELINE.md` — **§1** (rescope + decision 4: **rdflib `==7.6.0`**, standard Store API + SPARQL 1.1 → Oxigraph one-line swap; Neo4j rejected GPL-3.0; the **circularity warning**), **§4** (the table schema the graph is seeded from), **§6** (frozen GlobalId anchors + the 220-row oracle; **all real occupancy is flat-substring-decidable**), **§7** (cross-lingual debt boundary — what may/may-not anchor to Art.1). `docs/decisions.md` — **ADR-004** (honest-deferral precedent), **ADR-005** (Stage-4 split + the honesty boundary). `ROADMAP.md` — **Stage 4b** + the two-touch protocol (`:6-9`). `CLAUDE.md`.
Code you read/edit:
- `sandbox/checker.py` — `classify` (`:394-404`, the substring branch + accessory-first precedence + strict `unknown` complement — **REPLACED**); `load_applicability`/`_applicability` (`:328-391`, the table loader + cache — **kept as the seed**, and the model for the lazily-built ontology cache); `check_space` (`:421-444`, calls `classify` then `thr.resolve`); `SpaceFinding.compliant` keystone (`:241-252`, **byte-UNTOUCHED**); `run` (`:524-561`) / `main` exit (`:595`).
- `sandbox/rules/applicability.json` (hint groups + `provenance art1`|`cross-lingual-glossary` — **read, not edited**); `sandbox/rules/dm_1975_salva_casa.md` (Art.1 `:9-10`); `sandbox/parser.py` `verify_accessory_selection_against_text` (`:432-501` — the statute anchor for the 4 art1 tokens; it validates the *vocabulary list*, not any fixture room — note that bound).
- `sandbox/tests/test_applicability_table.py` (the 220-row equivalence + hint-set guards — **must stay green through the graph path**, and it calls `classify(_FakeSpace)`/`check_space` **outside any `run()`** — see the two-store rule); `sandbox/tests/equiv_oracle.json` (the frozen golden; occupancy label is element **`[0]`** per `fixture|salva_casa|GlobalId` — the **independent oracle** for TASK 1).

## START CONTROLS (run first from `sandbox/` — if any is off, STOP: env/regression, not Stage 4b)
- `python -c "import rdflib; print(rdflib.__version__)"` → **7.6.0**.
- `python tests/test_gate.py` → **37/37**; `test_requirement_model.py` → **25/25**; `test_height_keys.py` → **9/9**; `test_geometry_fallback.py` → **12/12**; `test_applicability_table.py` → **18/18, 0 skipped** (220-row 0-drift).
- `python checker.py data/AC20-FZK-Haus.ifc` → **5**; `--salva-casa` → **1**. `…AC20-Institute-Var-2.ifc` → **2** (**402/403**); `--salva-casa` → **2**. `…Duplex_A_20110907.ifc` → **0 / 21 undetermined** (both modes).
- `python probe_controls.py` → **FROZEN CONTROLS HELD**. If a `data/*.ifc` is missing, re-acquire per `stage32ndpart.md:22-27`.

## HARD RULES
- **TWO stores, do not conflate** (pre-review L2 — otherwise the kept tests RAISE). (i) A lazily-built, cached **ONTOLOGY graph** (hints + `rdfs:subClassOf` taxonomy) — this is what `occupancy_via_graph`/`classify` query; it must build **without a `run()`** (mirror `_applicability()` caching) so `classify(_FakeSpace)` and `check_space` work standalone. A **failed/empty ONTOLOGY build → RAISES** (fail-closed). (ii) A per-`run()` **IfcSpace store** — the room nodes, for the room-in-store proof. `classify` queries the ontology; it does **not** require the per-run store.
- **`classify()` is REPLACED, not shadowed.** Delete the Python substring branch + the precedence `if`s (`checker.py:400-403`); the body delegates to `occupancy_via_graph`. **Precedence + strict-complement live in the SPARQL**, not a Python tie-break: use a **single `SELECT` with a priority `BIND(IF(?cls=acc:Accessory,0,1) AS ?prio) … ORDER BY ?prio LIMIT 1`** (reference shape — proven 0/220 drift in the pre-review) so accessory-first is in the query. A no-match label → `unknown` (the strict complement); an **empty/failed ontology → RAISE** (these are distinct — assert both).
- **The IfcSpace must enter the per-run store**, materialized each `run()` (`:name`/`:longName`, + Qto height/area where present). Reference space nodes via **`initBindings`/`URIRef`/full `<IRI>` — NEVER a `:space_<GlobalId>` prefixed name in query text** (GlobalId `$` is URI-valid but illegal in a SPARQL `PN_LOCAL`; pre-review L4). Cache the ontology once; only insert/clear IfcSpace nodes per run (pre-review L5).
- **Controls byte-frozen THROUGH the graph path — NECESSARY-BUT-INSUFFICIENT.** FZK **5→1**, Institute **2→2** (`0jbV$RErb7o9P7rp7ALEd$`=402 / `3txvJd9V1BPhyU$48F$mnF`=403, both modes), Duplex **0/21** (both modes); the **220-row equivalence 0-drift**; the **402/403 canary `unknown` VIA THE QUERY**. Reproducing these proves a faithful copy only — the sufficiency comes from the statute anchor + the divergence room.
- **Anti-circularity, ENFORCED not asserted** (the pre-review's core fixes): (a) the TASK-1 reproduction battery is pinned to **`equiv_oracle.json[key][0]`** (real fixtures) and the **statute gate output** (Art.1 terms) — **NEVER an inlined map of the graph's own output**; (b) the divergence comparator is the **real `classify()`** (or the verbatim `any(h in label for h in table.accessory_hints)` lifted from `checker.py:400`), and the divergence token is **machine-asserted a non-substring of all 51 hints** and its `subClassOf` edge **proven load-bearing** (remove edge → `unknown`). A hand-rolled weak comparator or a substring-containing token (e.g. `Antibagno`, which contains `bagno`) is a **strawman → forbidden**.
- **Honest, BOUNDED claim only.** ADR-006 + the test docstrings state **"statute-anchored: 4/51 tokens; the other 47 reproduced-not-verified; circularity bounded, not eliminated; on the 3 fixtures the graph is verdict-equivalent to the flat table by construction; transitive inference demonstrated on a synthetic room."** No prose that implies the circularity is broken or the graph is non-cosmetic at production relevance.
- **Keystone byte-UNTOUCHED.** `SpaceFinding.compliant` (`:241-252`) + the undetermined exit (`:595`) unchanged; no-match → `unknown`/`undetermined`, never a pass.
- **rdflib `==7.6.0`**, standard Store API + SPARQL 1.1 only (Oxigraph swap preserved; Neo4j rejected). **Monostanza values stay OUT of the graph.** **Do NOT edit** the law `.md`, `tests/equiv_oracle.json` (the frozen golden — reproduce, never re-capture), or `applicability.json` content (the seed). Fix the code, not the harness. **One task at a time, lowest→highest risk; full REGRESSION BLOCK after each; any frozen control moves → revert.** Cite `file:line`.

## REGRESSION BLOCK (run after EVERY code task — from `sandbox/`)
```
python -c "import rdflib; print(rdflib.__version__)"                 # 7.6.0
python tests/test_graph.py                                           # NEW (grows, all green)
python tests/test_gate.py                                            # 37/37
python tests/test_requirement_model.py                              # 25/25
python tests/test_height_keys.py                                    # 9/9
python tests/test_geometry_fallback.py                              # 12/12
python tests/test_applicability_table.py                            # 18/18, 0 skipped (220-row 0-drift THROUGH the graph)
python checker.py data/AC20-FZK-Haus.ifc            --json data/AC20-FZK-Haus_report.json            # viol 5
python checker.py data/AC20-FZK-Haus.ifc --salva-casa --json data/AC20-FZK-Haus_report_sc.json       # viol 1
python checker.py data/AC20-Institute-Var-2.ifc     --json data/AC20-Institute-Var-2_report.json     # viol 2 (402/403)
python checker.py data/AC20-Institute-Var-2.ifc --salva-casa --json data/AC20-Institute-Var-2_report_sc.json # viol 2
python checker.py data/Duplex_A_20110907.ifc        --json data/Duplex_A_20110907_report.json        # 0 / 21 undet
python checker.py data/Duplex_A_20110907.ifc --salva-casa --json data/Duplex_A_20110907_report_sc.json # 0 / 21 undet
python probe_controls.py                                             # FROZEN CONTROLS HELD
```
**Global invariant:** `test_graph`/`test_gate` only **grow** (green); every other number **byte-identical** to START CONTROLS (FZK 5→1, Institute 2→2 on 402/403, Duplex 0/21 both modes; 220-row 0-drift; `thresholds`/`monostanza` blocks byte-identical). Any movement = revert.

---

## TASK 1 — Read-only graph layer: cached ONTOLOGY (hints + Art.1 statute anchor + `subClassOf` taxonomy) + a SPARQL `occupancy_via_graph`, proven non-vacuously  *(new `sandbox/graph.py` + `tests/test_graph.py`; checker UNTOUCHED → verdict-inert)*
- **Target:** new `sandbox/graph.py`, new `tests/test_graph.py`. **Do NOT wire into `checker.py` yet.**
- **Build (single concern — the ontology + the query, rdflib in-memory, lazily cached):**
  - Namespace `ACC`; classes `ACC.Accessory`/`ACC.Habitable`. Build from `applicability.json` (stdlib json, **NOT `import checker`**): each hint → a `:hintText` literal `:broaderTerm` its class, tagged with its `provenance`. Mark the 4 `art1` accessory tokens, cross-checked against the Art.1 prose via `parser.verify_accessory_selection_against_text` (a fabricated token RAISES, fail-closed), `:statuteAnchored true`; mark every `cross-lingual-glossary` edge `:declaredDebt true` and **never** `:statuteAnchored`.
  - A small `rdfs:subClassOf` taxonomy whose terminal parents are the statute-anchored Art.1 classes (e.g. `:Disimpegno rdfs:subClassOf ACC.Accessory`), plus **one divergence sub-type that is a non-substring of every hint** (see acceptance 2 — NOT `Antibagno`).
  - `occupancy_via_graph(name, long_name) -> "accessory"|"habitable"|"unknown"`: a **single SPARQL 1.1 `SELECT`** over the cached ontology — lower-case label `"<name> <long_name>"`, match a class if a `:hintText` is `CONTAINS`-in-label **OR** a room-type node reachable by `rdfs:subClassOf+`; **accessory-first via a priority `BIND`+`ORDER BY`+`LIMIT 1`**; no row → `unknown`. A failed/empty **ontology build RAISES** (distinct from the `unknown` no-match).
- **Expected verdict delta:** NONE (checker untouched).
- **Acceptance (`tests/test_graph.py`; non-vacuous, pinned to author-INDEPENDENT sources):**
  1. **Reproduces ground truth (necessary), pinned to the FROZEN golden — not the graph's own output:** load `tests/equiv_oracle.json`; for every real fixture space, `occupancy_via_graph(Name, LongName) == oracle["{fx}|False|{GlobalId}"][0]`. (The occupancy column was captured pre-refactor; `applicability.json`/`equiv_oracle.json` are un-editable, so this cannot be retro-fit.) For the Italian Art.1 terms not in any fixture, pin to the gate: for `tok` in `verify_accessory_selection_against_text(_ART1_GROUP, LAW)["anchored"]`, `occupancy_via_graph(tok,"") == "accessory"`. **Do NOT** assert against a hand-typed map.
  2. **Out-infers the REAL flat lookup (sufficient — the divergence room), with three machine guards:** pick a token (e.g. `Vorraum`/`Vestibolo`/`Putzraum`) and assert: (a) `not any(h in token.lower() for h in load_applicability().accessory_hints + habitable_hints)` — proven non-substring of all 51 hints (so a faithful flat lookup genuinely returns `unknown`); (b) the **real** comparator returns `unknown`: either `checker.classify(_FakeSpace(token))=="unknown"` OR the verbatim `any(h in token.lower() for h in accessory_hints)` is `False` (NOT a hand-rolled strawman); (c) `occupancy_via_graph(token,"")=="accessory"` via `subClassOf+`, AND **removing the `subClassOf` edge flips it back to `unknown`** (proves the transitive inference is load-bearing, not an incidental hint match).
  3. **Statute anchor + over-claim guard:** the 4 art1 terms are `:statuteAnchored`; a fabricated art1 token makes the build RAISE; every `cross-lingual-glossary` edge is `:declaredDebt` and NOT `:statuteAnchored`. Assert the **count**: exactly **4** `:statuteAnchored` tokens of **51** total (pins the bounded claim).
  4. **Fail-closed, two distinct paths:** a no-match label → `unknown`; a **failed/empty ontology → RAISES** (not a silent `unknown`).
- **REGRESSION BLOCK** (checker numbers byte-identical; `test_graph` green).

## TASK 2 — Wire the ontology query into the verdict path: REPLACE `classify()`, materialize the IfcSpace, prove the verdict flowed THROUGH the graph  *(checker.py; verdict-neutral on the 3 fixtures by construction)*
- **Target:** `checker.py` `classify` (`:394-404`), `run` (`:524`); `tests/test_graph.py`, `tests/test_applicability_table.py`.
- **Change (single concern):**
  - `classify(space)` body → `occupancy_via_graph(space.Name, space.LongName)` over the **cached ontology** (Task-1). **Delete** the substring branch + precedence `if`s. `load_applicability`/`_applicability` stay as the ontology seed (so `test_applicability_table` hint-set guards pass); `check_space` shape unchanged.
  - On `run()`, materialize each `IfcSpace` into the **per-run store** (nodes by `URIRef`, never a prefixed name).
  - Fail-closed preserved: no-match → `unknown`; failed ontology → RAISE.
- **Expected verdict delta:** NONE (same seed → identical per-space occupancy → 220-row + controls reproduce through the query).
- **Acceptance (the cosmetic/letter guards — output-equivalence is NOT enough):**
  - **220-row 0-drift with `classify` graph-backed**, and **402/403 `unknown` via the query** (targeted assertion).
  - **Verdict provably flows through the graph:** a test that **empties/breaks the ontology and asserts `classify`/`check_space` RAISES** (if it still returns a label, a Python branch is alive) **AND** `assert "in label" not in inspect.getsource(checker.classify)` (the substring branch is physically gone).
  - **Room-in-store, count-exact:** after `run(<fixture>)`, the per-run store's space-node GlobalId set `==` `{s.GlobalId for s in model.by_type("IfcSpace")}` (not merely "non-empty" — a 1-junk-node store must fail).
  - Keystone byte-identical: machine-check no finding reads `compliant==True` with an applicable check `None`.
- **Invariant (HARD):** any per-space verdict / count / GlobalId set / 220-row drift → revert.

## TASK 3 — Anti-circularity close-out, pin, docs (HONEST + QUANTIFIED)  *(tests + docs; no verdict)*
- **Tests:** the sufficiency legs are the Task-1 acceptance-2 divergence (load-bearing `subClassOf`) + the statute-anchored 4/51 — assert them as **runnable** checks, not comments. Add an explicit assertion that the reproduction battery (acceptance 1) is sourced from `equiv_oracle.json`/the gate, not the graph.
- **Dependency:** pin `rdflib==7.6.0` in the project manifest (standard Store API + SPARQL 1.1; Oxigraph documented swap; Neo4j rejected).
- **`docs/decisions.md` ADR-006** (append; never edit a past ADR): Stage 4b = the room→occupancy decision now flows from a **SPARQL 1.1 query over an rdflib `==7.6.0` store into which each IfcSpace is materialized at runtime**; `classify()`'s substring branch **replaced** (proven via raise-on-empty-ontology + source-absent, not just output-equivalence); controls byte-frozen through the graph path. **HONESTY (verbatim, the pre-review's core fix):** "On the 3 fixtures the graph is **verdict-equivalent to the flat table by construction** (all real occupancy is flat-substring-decidable, baseline §6) — reproducing the controls is **necessary-but-insufficient**. Non-circularity is **bounded, not eliminated**: **4 of 51** occupancy tokens are statute-anchored (Art.1 gate); the other **47 are reproduced, not independently verified**; cross-lingual hints remain declared-debt edges. The graph's added value is **(1) the architectural seam (room-in-store, query-driven verdict), (2) bounded statute-anchoring, (3) a transitive-inference capability demonstrated on a SYNTHETIC divergence room** — no real fixture needs it. The **scale trigger (~150 rules) has NOT fired**; this is seam-establishment, not production inference. The graph carries **occupancy only**; requirement values + monostanza wiring are later stages." Status `ACCEPTED`, HEAD per CLAUDE.md.
- **`ROADMAP.md`:** two-touch — flip **Stage 4b** Status (🔴→🟢; **✅** deferred to the scale/inference load that justifies a production graph) + at-a-glance row; one Iteration-Log line (newest at top), carrying the bounded/honest framing.
- **`.idos/events.jsonl`:** append a line **only** on a real STOP/GATE/DEFECT.
- **Invariant:** docs/tests only; REGRESSION BLOCK still green.

---

## DECIDE / HAND-OFF
- Stage 4b establishes the **graph seam honestly**: the IfcSpace enters an rdflib store, the room→occupancy verdict flows from a **SPARQL query** that **replaced** `classify()` (proven through-the-graph, not just output-equal), controls byte-frozen, with non-circularity **bounded (4/51 statute-anchored)** and a **load-bearing synthetic divergence room** demonstrating transitive-inference capability.
- **Honesty boundary (carry, do NOT over-claim):** (i) verdict-equivalent to the flat table by construction on the 3 fixtures — the controls are necessary-but-insufficient; (ii) **4/51 statute-anchored**, 47 reproduced-not-verified, cross-lingual = declared debt (baseline §7); (iii) inference shown **synthetically** (no fixture needs it, baseline §6) — value is seam + bounded-anchor + capability, **not** production inference; (iv) **scale trigger not fired**; (v) requirement VALUES + monostanza stay **out of the graph** (the un-gate-checked constants must not become graph "facts"; ADR-005 (ii)).
- **If TASK 2 entangles a frozen control** (e.g. the two-store split or per-run insertion regresses a number, or the SPARQL cannot reproduce a fixture's occupancy byte-for-byte): keep the proven verdict-inert TASK-1 layer, **leave `classify()` Python-backed**, and **STOP and report** — split the wiring to a 4b Part 2 rather than fake a control or strawman a test.
- **Next (NOT here):** ingest requirement VALUES into the graph (after wiring `verify_monostanza_against_text` to the records); room-type-hierarchy / multi-jurisdiction rules at scale; the Oxigraph backend swap.

## DONE-WHEN (Stage 4b)
1. `sandbox/graph.py`: cached ontology seeded from `applicability.json`, **4/51 accessory tokens statute-anchored** via `verify_accessory_selection_against_text`, cross-lingual edges **declared-debt**, a `subClassOf` taxonomy; `occupancy_via_graph` answers via a **single priority-`ORDER BY` SPARQL 1.1 SELECT**, fail-closed (ontology-build RAISE distinct from no-match `unknown`). `tests/test_graph.py` proves: reproduction pinned to **`equiv_oracle.json[key][0]` + the statute gate** (NOT the graph's own output); a **non-substring, load-bearing divergence room** vs the **real** `classify()`; the statute-anchor **count = 4/51**; both fail-closed paths.
2. `checker.py`: `classify()` substring branch **REPLACED** by the ontology query (proven via **raise-on-empty-ontology + `inspect.getsource` substring-absent**, not just output-equivalence); each IfcSpace **materialized into the per-run store**, asserted **GlobalId-set-exact**; `SpaceFinding.compliant` + the undetermined exit **byte-untouched**; no-match → `unknown`/`undetermined`, never a pass; space nodes referenced via `URIRef`/`initBindings` (never a `:space_<gid>` prefixed name).
3. REGRESSION BLOCK green after every code task: `test_graph` grows green, `test_gate` 37, `test_requirement_model` 25, `test_height_keys` 9, `test_geometry_fallback` 12, `test_applicability_table` 18/18 (**220-row 0-drift THROUGH the graph**), `probe_controls.py` HELD; FZK 5→1, Institute 2→2 (402/403), Duplex 0/21 both modes; `thresholds`/`monostanza` byte-identical.
4. **Non-circularity is BOUNDED and ENFORCED**: reproduction pinned to the frozen golden + gate (not the graph); the **402/403 canary `unknown` via the query**; a **machine-proven non-substring, load-bearing** divergence room vs the real flat lookup; "reproduces the controls" recorded as **necessary-but-insufficient** as a runnable check, not a comment.
5. **rdflib `==7.6.0`** pinned (SPARQL 1.1; Oxigraph swap; Neo4j rejected); **monostanza values OUT of the graph**; law `.md` / `applicability.json` / `equiv_oracle.json` **UNTOUCHED**.
6. **ADR-006 written + ROADMAP Stage 4b flipped**, with the **verbatim quantified honesty** (4/51 anchored; verdict-equivalent-by-construction on the 3 fixtures; inference synthetic; scale-trigger-not-fired; occupancy-only). `.idos/events.jsonl` appended **only** on a real STOP/GATE/DEFECT.
Then **STOP** and hand back the diff + regression output, and confirm the remaining scope (requirement-values-in-graph + monostanza-gate-to-records + Oxigraph-at-scale) is **not** started.
